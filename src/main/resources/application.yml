# application.yml c?a Config Server
server:
  port: 8888 # C?ng m?c ??nh c?a Config Server

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/vietluannguyen711/microservices-configuration.git # <-- Thay b?ng URL kho Git c?a b?n
          default-label: main # Tùy ch?n: Nhánh Git m?c ??nh (ví d?: main, master)
          clone-on-start: true